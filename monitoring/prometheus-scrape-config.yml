# Monitoring Enhancement: Prometheus Scrape Configuration for reviews
# Add this job to your prometheus.yml scrape_configs section.
scrape_configs:
  - job_name: 'reviews'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 15s
    scrape_timeout: 10s
    static_configs:
      - targets:
          - 'localhost:{{ PORT }}'   # Replace with actual host:port
        labels:
          service: 'reviews'
          environment: 'production'
